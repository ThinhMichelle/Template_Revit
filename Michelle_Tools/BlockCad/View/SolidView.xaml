<Window x:Class="BlockCad.View.SolidView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             mc:Ignorable="d" 
        Title="Rebar Solid"
             Height="140" Width="300"
        WindowStartupLocation="CenterScreen">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="5,15,5,15">
            <CheckBox Content="Solid In View" IsChecked="{Binding IscheckedSoild}"/>
            <CheckBox Content="Unobscured In View In View" IsChecked="{Binding IsCheckedUnobscured}"/>
        </StackPanel>
        <Button Content="run" Grid.Row="1" Height="30" Command="{Binding ButtonRun}"/>
    </Grid>
</Window>
